#define optimize() ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#include <bits/stdc++.h>
using namespace std;
int n,k;
string path;


vector<string>s;

bool find_b(int i,int j)
{
    int o=0;
    
    if(s[i][j]=='#')return 0;
    if(s[i][j]=='.')return 1;
    
    if(s[i][j])
    
}


void work() {
    cin>>n>>k;
    for(int i=0;i<n;i++)
    {
        string a;
        cin>>a;
        s.push_back(a);
    
    }
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<k;j++)
        {
            if(s[i][j]=='A')
        }
    }
    
}

int main() {
    optimize(); 
    
        work(); 
    return 0;
}

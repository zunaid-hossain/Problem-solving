#define optimize() ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#include <bits/stdc++.h>
using namespace std;

void work() {
    string a,b;
    cin>>a>>b;
    int s = b.size();
    
    for(int i=0;i<a.size();i++)
    {
        if(a[i]==b[0])
        {
            for(int j=0;j<s;j++)
            {
                if(a[i])
            }
        }
    }

}

int main() {
    optimize(); 
    int t;
    cin >> t;  
    while (t--) {
        work(); 
    }
    return 0;
}

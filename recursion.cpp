#define optimize() ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#include <bits/stdc++.h>
using namespace std;

int s=0;

int rm(int s ,int e){
    if()
}
void work() {
    int n;
    cin>>n;
    cout<<rm(n)<<endl;

}

int main() {
    optimize(); 
    int t;
    cin >> t;  
    while (t--) {
         
      work();
    }

    return 0;
}

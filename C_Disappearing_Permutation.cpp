#define optimize() ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#include <bits/stdc++.h>
using namespace std;

void work() {
    int n;
    cin>>n;

    vector<int>a(n);
    vector<int>d(n);
    vector<int>n(n);
    vector<int>ans(n);

    for(auto &i:a)
    {
        cin>>i;
    }
    for(auto &i:d)
    {
        cin>>i;
    }

}

int main() {
    optimize(); 
    int t;
    cin >> t;  
    while (t--) {
        work(); 
    }
    return 0;
}
